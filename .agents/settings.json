{
  "enableTelemetry": false,
  "model": "Claude Opus 4.6 (Thinking)",
  "trustedWorkspaces": [
    "/Users/aobaiwaki",
    "/Users/aobaiwaki/ai-workspace",
    "/Users/aobaiwaki/lumiscrape",
    "/home/aoba/ai-workspace"
  ],
  "permissions": {
    "allow": [
      "command(git( .*)?)",
      "command(gh( .*)?)",
      "command(ls( .*)?)",
      "command(pwd)",
      "command(cd( .*)?)",
      "command(tree( .*)?)",
      "command(cat( .*)?)",
      "command(head( .*)?)",
      "command(tail( .*)?)",
      "command(grep( .*)?)",
      "command(find( .*)?)",
      "command(which( .*)?)",
      "command(wc( .*)?)",
      "command(echo( .*)?)",
      "command(mkdir( .*)?)",
      "command(chmod( .*)?)",
      "command(curl( .*)?)",
      "command(jq( .*)?)",
      "command(sed( .*)?)",
      "command(awk( .*)?)",
      "command(sort( .*)?)",
      "command(uniq( .*)?)",
      "command(cut( .*)?)",
      "command(tr( .*)?)",
      "command(tee( .*)?)",
      "command(date( .*)?)",
      "command(env( .*)?)",
      "command(export( .*)?)",
      "command(xargs( .*)?)",
      "command(base64( .*)?)",
      "command(sleep( .*)?)",
      "command(test( .*)?)",
      "command(true)",
      "command(false)",
      "command((.*)?python3?( .*)?)",
      "command(.*\\.venv/bin/.*)",
      "command(uv( .*)?)",
      "command(ruff( .*)?)",
      "command(mypy( .*)?)",
      "command(pytest( .*)?)",
      "command(uvicorn( .*)?)",
      "command(scrapy( .*)?)",
      "command(node( .*)?)",
      "command(npm( .*)?)",
      "command(npx( .*)?)",
      "command(go( .*)?)",
      "command(cargo( .*)?)",
      "command(rustc( .*)?)",
      "command(kubectl( .*)?)",
      "command(k0sctl( .*)?)",
      "command(k8sctl( .*)?)",
      "command(helm( .*)?)",
      "command(mc( .*)?)",
      "command(bash( .*)?)",
      "command(zsh( .*)?)",
      "command((bash )?(\\./)?.*scripts/.*)",
      "command((/usr/lib/wsl/lib/)?nvidia-smi( .*)?)"
    ],
    "deny": [
      "command(rm -rf /)",
      "command(rm -rf ~.*)",
      "command(rm -rf \\*)",
      "command(git push.*--force.*)",
      "command(git push.*-f .*)",
      "command(git reset --hard.*)",
      "command(git clean -fd.*)",
      "command(sudo .*)",
      "command(mkfs.*)",
      "command(dd .*)"
    ]
  }
}
